{% assign main_collection = product.collections.first %}
{% assign width = section.settings.width %}

{% style %}
  #product-recommendations {
    padding: var(--global-p2);
    display: flex;
    gap: var(--global-p1);
    flex-direction: row;
  }
  @media (max-width: 490px) {
    #product-recommendations {
      padding: var(--global-p1);
      flex-direction: column;
    }
    .product-grid {
      padding: 0 !important;
    }
{% endstyle %}

<!-- Product Recommendations -->
{% if section.settings.show_section %}
  <div
    id="product-recommendations"
    class="section is-width-{{ width }} product-recommendations"
    data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit={{ section.settings.product-limit }}&intent={{ section.settings['product-type'] }}"
  >
    <header>
      <h2>{{ section.settings['recommendation-title'] }}</h2>
      <h3>{{ section.settings['similar-products-title'] }}</h3>
    </header>

    <div class="product-grid">
      {% if section.settings['product-type'] == 'related' %}
        {% for product in recommendations.products limit: section.settings['product-limit'] %}
          {% include 'product-card',
            show_show_product_description: section.settings.show_description,
            button_label: section.settings.button_label,
            button_class: section.settings.button_class,
            button_style: section.settings.button_style,
            card_width: section.settings.card_width
          %}
        {% endfor %}
      {% elsif section.settings['product-type'] == 'complementary' %}
        {% for product in recommendations.products limit: section.settings['product-limit'] %}
          {% include 'product-card',
            show_show_product_description: section.settings.show_description,
            button_label: section.settings.button_label,
            button_class: section.settings.button_class,
            button_style: section.settings.button_style,
            card_width: section.settings.card_width
          %}
        {% endfor %}
      {% else %}
        {% for product in main_collection.products limit: section.settings['product-limit'] %}
          {% include 'product-card',
            show_show_product_description: section.settings.show_description,
            button_label: section.settings.button_label,
            button_class: section.settings.button_class,
            button_style: section.settings.button_style,
            card_width: section.settings.card_width
          %}
        {% endfor %}
      {% endif %}
    </div>
  </div>
{% endif %}

<style>
  header{
    font-size: {{ section.settings.header_font_size }};
  }
</style>
{% schema %}
{
  "name": "Product Recommendations",
  "class": "product-recommendation-section",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_section",
      "label": "Show Recommended Products Section",
      "default": true
    },
    {
      "type": "text",
      "id": "recommendation-title",
      "label": "Product Recommendations Title",
      "default": "Product Recommendations"
    },
    {
      "type": "range",
      "id": "header_font_size",
      "label": "Header Title Size",
      "info": "multiplies the size of base text size by the value divided by 10 [base size * (value / 10)]",
      "default":30,
      "min": 5,
      "max": 50,
      "step": 1
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "width",
      "label": "Width",
      "default": "standard",
      "options": [
        {
          "value": "half",
          "label": "Half"
        },
        {
          "value": "standard",
          "label": "Standard"
        },
        {
          "value": "wide",
          "label": "Wide"
        }
      ]
    },
    {
      "type": "checkbox",
      "id": "similar-products",
      "label": "Show Similar Products",
      "default": true
    },
    {
      "type": "radio",
      "id": "product-type",
      "label": "Product Type",
      "options": [
        {
          "value": "complementary",
          "label": "Complementary Products"
        },
        {
          "value": "related",
          "label": "Related Products"
        },
        {
          "value": "collection",
          "label": "Collection"
        }
      ],
      "default": "complementary"
    },
    {
      "type": "header",
      "content": "Product Card",
      "info": "Save the page to reload the cards, sometimes you might need to reload the page, for changes to appear."
    },
    {
      "type": "text",
      "id": "similar-products-title",
      "label": "Similar Products Title",
      "default": "Similar Products"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label"
    },
    {
      "type": "select",
      "id": "button_class",
      "label": "Button Class",
      "options": [
        {
          "value": "btn",
          "label": "Default Button"
        },
        {
          "value": "btn-primary",
          "label": "Primary Button"
        },
        {
          "value": "btn-secondary",
          "label": "Secondary Button"
        }
      ]
    },
    {
      "type": "select",
      "id": "button_style",
      "label": "Button Style",
      "options": [
        {
          "value": "btn--small",
          "label": "Small Button"
        },
        {
          "value": "btn--large",
          "label": "Large Button"
        }
      ]
    },
    {
      "type": "range",
      "id": "card_width",
      "label": "Card Width",
      "min": 100,
      "max": 360,
      "step": 20,
      "default": 260
    },
    {
      "type": "checkbox",
      "id": "show-product-image",
      "label": "Show Product Image",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_title",
      "label": "Show Product Title",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_price",
      "label": "Show Product Price",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_description",
      "label": "Show Product Description",
      "default": false
    },
    {
      "type": "range",
      "id": "product-limit",
      "label": "Product Limit",
      "min": 1,
      "max": 8,
      "step": 1,
      "default": 4
    }
  ],
  "presets": [
    {
      "name": "Product Recommendations"
    }
  ]
}
{% endschema %}

{% javascript %}
  const handleIntersection = (entries, observer) => {
    if (!entries[0].isIntersecting) return;
    observer.unobserve(productRecommendationsSection);
    const url = productRecommendationsSection.dataset.url;
    fetch(url)
      .then(response => response.text())
      .then(text => {
        const html = document.createElement('div');
        html.innerHTML = text;
        const recommendations = html.querySelector('.product-recommendations');
        if (recommendations && recommendations.innerHTML.trim().length) {
          productRecommendationsSection.innerHTML = recommendations.innerHTML;
        }
      })
      .catch(e => {
        console.error(e);
      });
  };
  const productRecommendationsSection = document.querySelector('.product-recommendations');
  const observer = new IntersectionObserver(handleIntersection, {rootMargin: '0px 0px 200px 0px'});
  observer.observe(productRecommendationsSection);
{% endjavascript %}
