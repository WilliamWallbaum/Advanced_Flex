<p>{{ product | json }}</p>
{% for variant in product.variants %}
  {% assign variant_title = variant.title | downcase %}
  {% if variant_title contains 'single' and pair_type == 'single' %}
    <h5>{{ variant_title }}</h5>
  {% elsif variant_title contains 'pair' and pair_type == 'single' %}
    <h5>{{ variant_title }}</h5>
  {% endif %}
{% endfor %}
